# 视觉流水线

后续在此增加 mark、component、feeder 等流水线，实现 `ports.vision.Locator`。
输入图像与配方，输出像素位置、角度及算法定义的质量指标；失败应明确返回或抛出领域异常。
算法函数保持独立，不导入 machine、api 或具体串口驱动。

OpenCV 尚未引入。接入时在目标 ARM64 系统确定兼容版本，并在 worker 中执行计算。
帧所有权必须明确，正在识别的图像不能被采集线程覆盖。
