# 标定

后续分别实现相机内参/畸变、像素到工作平面、PCB 到机器、相机到吸嘴偏移。
显式记录输入输出坐标系、单位、标定版本与适用工作高度。
不提供默认恒等变换，缺失标定必须拒绝视觉引导运动。
当前演示只报告像素坐标，不做位置补偿、不执行贴装。

第一版双相机：下视相机需要像素/工作平面映射与头部偏移；上视相机需要固定拍照位置、
像素映射及吸嘴旋转轴标定。元件偏心随吸嘴旋转而改变，最终补偿需应用对应旋转。
两路相机分别保存标定数据，禁止互用；PCB 由夹具固定，再以 Mark 建立板到机器的变换。
